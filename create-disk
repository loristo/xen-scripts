#!/bin/sh

set -e

if [ "$UID" -ne 0 ]; then
    echo run as root
    exit 1
fi

if [ "$#" -ne 2 ]; then
    echo usage: create-disks vm-name disk-size
    exit 1
fi

lvcreate -n "$1-disk"  -L "$2" vg0
